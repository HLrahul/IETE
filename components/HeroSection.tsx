export default function Hero() {
    return (
        <div className="h-screen w-full relative p-10 pt-16">
            <h1 className="text-5xl">
                This is the Hero Section
            </h1>
            <canvas id="ieteObject"></canvas>
        </div>
    );
}
